# Static Assets 模块

静态资源目录，用于存放可通过 `/static/...` 访问的系统内置资源。

## 文件列表

- `system/avatars/default.svg` — 通用默认头像。
- `system/avatars/azure-ai.png` — Azure AI 助手固定入口默认头像。
- `system/avatars/system-notify.png` — 系统通知固定入口默认头像。
