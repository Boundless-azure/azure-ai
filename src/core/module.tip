Core 目录模块说明

模块列表：

- ai：AI 核心模块（AICoreModule）
  - 作用：管理 AI 模型、会话上下文与提示模板实体；提供与数据库/上下文相关的核心能力。

- tip：Tip 模块（TipModule）
  - 作用：提示词（Tip）的管理与生成，支持从文件收集与服务生成，便于统一维护提示内容。

- plugin：插件模块（PluginModule）
  - 作用：插件元数据、服务与关键词解析，支持业务插件的注册、读取和运行时交互。

- prompt：Prompt 渲染工具模块（PromptModule）
  - 作用：为一段 Prompt 模板提供数据渲染能力（支持 `${expr}`），数组/传值/函数渲染与严格 JSON-only 输出。

备注：
- 本文件用于对 core 下的各模块进行作用标记与目录索引，便于 IDE 与开发者快速了解结构。
- 仅文档用途，不参与构建流程。