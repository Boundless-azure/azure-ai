/**
 * @title Todo Status Enum
 * @description Status values for todos.
 * @keywords-cn 待办状态, 枚举
 * @keywords-en todo-status, enum
 */
export enum TodoStatus {
  Unread = 'unread',
  Read = 'read',
  Completed = 'completed',
  Rejected = 'rejected',
  Failed = 'failed',
}

