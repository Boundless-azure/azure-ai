# SaaS Caddy 配置
# HTTPS 终止并直接反代到 NestJS :3000

{
  admin off
}

:80, :443 {
  tls internal

  # 所有请求直接转发到 NestJS 后端
  # 携带原始 Host 头，供 404 转发过滤器识别域名
  reverse_proxy saas-backend:3000 {
    header_up Host {host}
    header_up X-Forwarded-Proto {scheme}
    header_up X-Real-IP {remote_host}
  }
}
