/**
 * @title Agent 运行时枚举
 * @description 描述加载状态枚举
 * @keywords-cn 枚举, 加载状态
 * @keywords-en enum, load-status
 */
export enum AgentLoadStatus {
  NotFound = 'not_found',
  Loaded = 'loaded',
  DialogueReady = 'dialogue_ready',
}
